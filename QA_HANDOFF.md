# Turf & Taste QA Handoff

## Active objective

Complete a whole-platform mobile-first UI, UX, workflow, and shared API QA cycle. Fix verified findings without regressing web or Capacitor clients.

## v2 rebuild continuation

- The governing scope is the latest master rebuild requirement. Detailed status, dependencies, and the module queue live in `docs/IMPLEMENTATION_PROGRESS.md`; the repository remains the source of truth.
- **Current checkpoint:** Modules 1–4 and 6–7 are QA verified; Module 5 is locally QA verified with its real Razorpay capture/replay test explicitly provider-blocked; Module 2’s management-security foundation is QA verified. Modules 6–7 retain one explicit external device exception below.

## 2026-09-23 Android / clean-install stabilization checkpoint

- **Branch:** `feature/turf-and-taste-v2` remains authoritative. Do not merge or develop on the Antigravity branch before its separate review.
- **Antigravity disposition:** `origin/feature/antigravity-module8-backfill` was reviewed as one isolated commit. Its useful implementation was cherry-picked and amended on this authoritative branch as `569c2e5`; it was not merged. The remote branch is retained as history, not deleted, because it contained integrated work.
- **Physical finding (user-reported):** Android at approximately **384 × 832 CSS pixels** surfaced a payment error that appeared to parse a DOCTYPE/HTML response.
- **Root cause fixed in source:** a Capacitor bundle with no `VITE_API_URL` used relative `/api`; the browser-only Vite proxy does not exist in the WebView, allowing the app shell/HTML to reach blind `response.json()` parsing. `src/services/api.js` now fails fast on native builds without an explicit endpoint and safely rejects non-JSON/malformed responses. Build mobile bundles with a reachable `VITE_API_URL` (HTTPS for deployed builds); never use device `localhost`.
- **Gateway QA:** an isolated disposable backend with configured Razorpay test credentials created a live test order from the server-signed quote and returned the server-derived ₹200 deposit. Invalid signature verification returned `400` before a booking transition. The gateway fallback no longer fabricates an order/key; missing gateway configuration gives a clear `503`, while manual UPI remains review-only. Successful provider capture, checkout callback, signature success, and replay against Razorpay remain externally dependent on completing a real test payment and must not be marked verified yet.
- **Customer identity audit:** there is no customer registration/login/session system yet. The Profile route is now visibly device-local booking details with no seeded Player/Member persona. Guest browsing/booking remains intended; customer identity, verified history, logout/session restoration, and synced profile are Module 13 dependencies. Phone/email history lookup is retained legacy behavior, not authentication.
- **384px browser QA actually performed:** Home, Facilities, valid and invalid Facility Detail, Booking, Inquiry, Pricing, Contact, About, My Bookings, and guest details were checked at an explicit 384 × 832 browser viewport. No document-level horizontal overflow was measured. Booking's clipped four-label stepper was fixed and visually rechecked. Profile edit modal controls stayed on top of the bottom tab bar. This is not physical Android evidence.
- **Current environment limitation:** `npx cap sync` cannot run because Capacitor 8 requires Node >=22 and this host has Node 20.20.2. Rebuild/install/device-test after Node upgrade, using an explicit reachable API URL, before calling the Android release flow verified.
- Module 2 is in progress. Admin login now uses a username-plus-IP keyed process-local throttle (8 attempts/15 minutes), returns `429`/`Retry-After`, and clears failures after a valid login. Isolated QA verified throttling, identity isolation, reset behavior, and JWT-protected admin access.
- Module 2 role/disablement foundation is now complete: migration `008_admin_roles` backfills existing administrators as enabled `super_admin` accounts and adds `manager` as the only other recognized role. Protected management requests resolve the current database account for every JWT, so disabled or unsupported-role accounts fail closed even with old tokens. `PUT /api/admin/accounts/:id/enabled` is super-admin only and prevents self-disablement/final-super-admin lockout. Disposable HTTP QA verified enabled login, manager access, manager escalation rejection, disabled-login rejection, old-token rejection across `/api/admin/verify` and `/api/bookings`, re-enablement, and unknown-role rejection. Next Module 2 slice: introduce a minimal server-side token/session invalidation version without breaking current JWT clients.
- Module 2 session invalidation is now complete: migration `009_admin_session_versions` adds a monotonic account version to new JWTs and verifies it from the authoritative account row on every protected request. Enabling/disabling an account and changing its password increment the version, invalidating every old token; versionless pre-migration tokens remain compatible only until such a security action. Disposable HTTP QA verified disabled old-token rejection, continued rejection after re-enablement, password-change revocation, old-password rejection, and new-password login. Next Module 2 slice: harden the existing management password-change path while preserving legacy password-hash compatibility.
- Module 2 password hardening is now complete: legacy plaintext seed values are timing-safe compared and upgraded to bcrypt cost 12 on successful login; new passwords are 10–128 characters, cannot reuse the current password, and revoke earlier sessions on change. Disposable HTTP QA verified legacy hash upgrade, weak/reused-password rejection, old-token invalidation, old-password rejection, and new-password login. Next Module 2 slice: add narrowly scoped audit records for important management-auth actions without creating a general-purpose logging subsystem.
- **Module 2 is QA verified.** Migration `010_admin_auth_audit` adds a focused, indexed server-side auth audit trail. It records login success, throttling, account enablement, role changes, password changes, and explicit session revocation using only actor/target IDs, action, outcome, timestamp, and allowlisted metadata. Its storage failure is non-critical, and it never records passwords, hashes, JWTs, reset tokens, or request payloads. Disposable HTTP QA verified all event types plus no sensitive-value leakage. Customer authentication remains intentionally deferred to Module 13. Next safe module: public Website / Facility Discovery reconciliation against managed facility data, retaining the richer static fallback until parity is confirmed.
- The current vertical API slice exposes a safe backend inventory at `GET /api/facilities` and `GET /api/facilities/:identifier`, populated from legacy facilities or, for SQLite development, the seeded pricing tiers. Public UI remains on its richer static dataset until facility content administration is implemented.
- V2 response conventions are now available through `server/utils/api.js`. The first public/admin setting boundary is `GET /api/v2/settings/public` and protected `PUT /api/v2/settings/public/public_contact`; isolated QA verified public reads, anonymous rejection, authenticated persistence, and readback.
- **Module 1 is QA verified.** Versioned migrations, public/admin settings boundaries, provider-neutral media metadata, and backward-compatible legacy booking behavior are established. Module 2’s next safe task is a minimal server-side session/token invalidation version, without beginning customer-account work.
- **Module 3 is in progress.** Facility management now has protected profile create/update and transactional weekly schedules. Isolated QA on 2026-09-22 verified anonymous `401`, valid create, duplicate-day schedule `400`, atomic schedule replacement, and public readback. Time values are minutes from midnight; overnight schedules are supported and no hours are hard-coded as business rules.
- Admin now has a dedicated Facilities tab backed by the protected inventory API, including profile and weekly-schedule editing. Public inventory no longer exposes drafts via a query parameter; isolated QA verified public draft exclusion, authenticated admin visibility, and anonymous admin-list `401`.
- Migration `005_backfill_rich_facility_content` now brings all seven existing public facility records into managed storage, including images, descriptions, rules, and metadata. It seeds sports/dining hours as editable defaults only and never overwrites existing schedules or administrator-authored descriptions. Isolated QA verified all seven records, seven schedule rows each, idempotency, and preservation behavior.
- Public Facilities discovery now reads managed API records and retains static data only as an API-failure fallback. The migration deliberately does not touch booking/pricing consumers; Facility Detail is the next public reader to migrate.
- Module 6–7 discovery reconciliation is in progress. Facility Detail now preserves its known static fallback when managed inventory is unavailable, while still honoring an authoritative successful not-found response. Discovery group counts now come from managed public inventory instead of fixed legacy values. Next safe slice: audit and normalize any remaining customer-facing static facility assumptions in Home and Inquiry without touching server-authoritative booking/pricing behavior.
- Module 6–7 Home/Inquiry reconciliation is complete: Home’s facility count, links, bookable quick-play list, and dining cards use managed public inventory with static enrichment/fallback; Inquiry exposes only managed active/bookable sports in its selector and keeps a graceful static fallback. Pricing, booking, quotes, and payments are unchanged. Next safe slice: audit static facility assumptions in navigation, About, Contact, and Pricing, then conduct the focused customer-facing UX review before considering Module 6–7 complete.
- Focused Module 6–7 UX review found and fixed stale customer-facing operating-hour claims. Home no longer claims 24-hour sport availability; Facility Cards and dining detail no longer hard-code hours that administrators can change. Public schedule messaging now directs customers to current venue details. Next: complete route-level visual/mobile/fallback review for the public surfaces, then mark Modules 6–7 only if no remaining material issue is found.
- Public route QA also found a fallback-only Facility Detail regression: static sports did not carry the newer `bookingEnabled` field and could render the dining/walk-in branch when the API was unavailable. The fallback now derives that state from its legacy category; live QA confirmed Box Cricket retains Book This Facility and Inquire for Events CTAs. The subsequent public route review is complete; the true 390px viewport execution remains tool-limited in this environment.
- **Modules 6–7 are QA verified with a device exception.** The public desktop route pass covered Home, Facilities, valid/invalid Facility Detail, Pricing, Inquiry, Contact, About, navigation, and booking-entry CTAs while the managed-inventory API was unavailable. Fallbacks and recovery CTAs remained usable, and the unknown-detail route now recovers as a true not-found state rather than an indefinite loader. Remaining fixed/24-hour schedule labels were removed from Booking, Pricing, Facility Detail, Contact, and footer content; schedule authority stays with per-facility configuration and the server-authoritative booking flow. Source inspection found no new public hard-width/overflow risk across the existing responsive grid and breakpoint rules. Accessibility snapshots confirmed named navigation and CTA/form labels. A real 390px physical/device-emulator visual pass was not possible with the current browser tooling and remains explicitly deferred. Next safe module: Module 8 — Food Court & Parlour, starting with additive protected food-stall/menu persistence and API contracts while retaining static presentation as a fallback.
- Module 8 foundation is complete: migration `011_food_menu_foundation` adds a single additive stall/category/item model shared by normal food stalls and the convenience parlour through `stall_type`. It stores prices as integer paise and includes publication, availability, dietary, featured, ordering, image, and metadata fields without introducing food ordering. Isolated SQLite migration apply/reapply plus build and syntax checks passed. Next safe slice: protected stall/category/menu-item CRUD followed by public active-only read APIs.
- Module 8 API slice is QA verified: `/api/v2/food` provides active-only public list/detail reads; protected `/admin` stall/category/item create/update endpoints preserve the shared parlour model and validate identity, pricing, state, ordering, dietary values, and category ownership. Public content is allowlisted and does not expose admin contact/status/availability fields. An isolated SQLite HTTP suite on a dynamically assigned loopback port verified anonymous `401`, active food plus draft parlour creation, category/item persistence, integer-paise pricing, public allowlisting, draft exclusion, and `404` behavior. Next safe slice: Admin food-management UI; preserve static dining presentation until managed seed parity is verified.
- Module 8 Admin Food UI is QA verified. The protected Admin Food & Parlour tab has create/edit workflows for shared food/parlour stalls, statuses, categories, and menu items; it converts rupee entry to integer paise and refreshes from the server after every success. Disposable authenticated browser QA verified empty/loading/success states, category edit/reload, item edit/reload, category reassignment, price persistence (₹63.25), vegetarian classification, availability/featured toggles, and inactive/draft visibility. Safe field-level stall validation details now identify only a field and a user-safe reason.
- **Module 8 dining content backfill is QA verified for SQLite.** Migration `012_backfill_dining_content` seeds 2 shared food/parlour stalls (`cafe`, `snack-parlours`), 6 categories, and 15 integer-paise menu items from static dining records. Its additive conflict guards preserve existing administrator records, including a competing slug, rather than overwriting them. Independent disposable SQLite QA verified fresh seed counts, idempotent reapplication, preservation of an intentionally edited café record, public active-only allowlisting, and inactive-detail `404`; static customer fallback is unchanged and no ordering/cart/checkout exists. PostgreSQL compatibility is source-reviewed only because its non-production endpoint remains unreachable. Next safe slice: progressively consume `/api/v2/food` in public Food Court/parlour presentation while retaining static fallback.
- **Module 8 public dining-detail slice is QA verified.** Café and snack-parlour Facility Detail pages now retrieve the public allowlisted `/api/v2/food/:identifier` response and display managed categories/items, integer-paise prices, dietary/featured labels, ordering, and operating-hour labels. The API JSON helper was corrected to preserve typed arrays, so persisted `operatingHours` now round-trip safely without relaxing object validation. The shared food/parlour panel uses accurate parlour-specific copy. Existing rich facility data is still the immediate API-outage/inactive-stall fallback; a disposable SQLite browser pass verified active Café at 384×832, active Parlour at 360×800, and inactive-Parlour fallback at 412×860 with no document-width overflow. Public response assertions confirmed no `status`, `contact`, `available`, or `active` management fields leaked. This is browser-only QA; physical Android verification remains pending the Node 22+ Capacitor build gate.
- **Module 8 public dining-discovery slice is QA verified.** Home and Facilities consume `GET /api/v2/food` only as an authoritative replacement for the dining subset after a successful response; failure deliberately leaves the enriched facility fallback intact. Shared IDs/slugs prevent duplicate cards, and a successful empty/inactive food list hides stale static dining cards. Disposable browser QA marked `snack-parlours` inactive and verified Café alone at 384×832 Home and 360×800 Facilities, with no horizontal overflow and a correct `/facilities/cafe` menu route. A separate browser frontend with an intentionally unreachable API verified both enriched static dining cards and their correct routes at 412px Home and desktop Facilities. Focused Food Court responsive/empty-state review remains before the public migration can be marked fully complete.
- **Module 8 is QA VERIFIED for locally testable web behavior.** The final public review added a concise managed-empty-menu state with a counter/contact recovery action, so an active stall with zero available public items is not mistaken for a loading or broken page. Managed active Café/Parlour, inactive hiding, categories/items, paise prices, dietary/featured state, operating hours, navigation, static outage fallback, and responsive browser widths were covered through disposable API/browser checks. No cart, checkout, ordering, customer authentication, or admin-only public data was introduced. PostgreSQL runtime remains unverified due to the unavailable non-production endpoint; Android Razorpay physical-device testing remains separately pending Node 22+ Capacitor rebuild/device execution. Next safe module: Module 9 Events CMS additive persistence/protected API foundation.
- Module 3 is now QA verified: Facility Detail also reads managed records with safe fallback and no longer silently substitutes a different venue for an invalid slug. Booking/pricing consumers remain deliberately unchanged and are the next Module 4 dependency.
- Module 4 is in progress. `POST /api/v2/quotes` is the first server-authoritative pricing boundary: it checks managed facility availability, schedule, conflicts, blocks, pricing, and weekend rules before returning total/deposit. Isolated QA covered valid, non-bookable, and out-of-schedule paths; legacy booking submission is unchanged pending the next migration slice.
- Public booking creation now enforces signed short-lived one-time quote tokens and records redemption atomically. Server amounts override browser values; isolated QA verified valid redemption, browser amount resistance, reuse `409`, and context-tampering `409`. Authenticated walk-ins and existing UPI/Razorpay state protections remain intact. Next: Module 5 gateway quote alignment and booking pass hardening.
- Module 5 is in progress. Razorpay order creation now requires the signed quote and derives deposit/full amount server-side; the client no longer submits an order amount. Next harden gateway verification against order/quote context and replay before creating customer-safe passes.
- Payment orders now persist signed quote context and expected server amount. Isolated QA confirmed a browser ₹1 amount became a ₹200 gateway order and missing quote context returns `400`. Next bind verification to this persisted context and prevent replay.
- Razorpay verification now uses persisted order/quote context, validates constant-time signature and provider order/paise amount, and atomically updates payment/order/redemption/booking state. Full live capture/replay QA requires a real successful Razorpay test payment; invalid/missing-signature and build checks remain locally testable.
- Customer booking pass UI now shows only safe booking/payment information, with clearly distinct confirmed versus payment-review states and accurate total/paid/balance display. Gateway identifiers, tokens, signatures, and customer phone numbers are not shown. Browser print is retained; download/share/QR were not added without a reliable implementation.
- Module 5 is locally QA verified: invalid UPI references do not consume quotes, valid retry stays `Payment Review` / `Pending verification` despite client paid/confirmed attempts, signed server deposit persists, and the customer pass has no sensitive identifier leakage. A real successful Razorpay test capture/replay remains an explicit external-provider dependency and is not simulated. Next safe module: minimum management-auth hardening (roles and sensitive-login rate limits).
- Verified on 2026-09-22: server syntax, a clean isolated SQLite application and repeat application of migration `001_v2_foundation`, `npm run build`, and `git diff --check`.
- PostgreSQL runtime execution remains environment-blocked because the configured endpoint is unreachable. The migration runner translates its bookkeeping placeholders for PostgreSQL; validate it against a reachable non-production database before production deployment.

## Completed in the current cycle

- Booking now requests live availability whenever facility, date, or duration changes.
- The API and server reject overlapping booking intervals, not just identical labels.
- Maintenance slots are non-selectable and have accessible status text.
- Direct UPI records a reference for staff review; it no longer claims automatic payment verification.
- Admin access validates a JWT; hard-coded browser credentials and privileged API gaps were removed.
- Build and server syntax checks passed. Local API checks verified public slots, protected management endpoints, and rejection of unconfigured gateway verification.
- Home discovery now derives hourly rates from the live pricing store and lists all seven facilities, including both dining offerings. It no longer promises a universal ₹200 deposit.
- Contact and group inquiry submissions now preserve the user's form and show an error if the server rejects or cannot receive the request; they no longer report simulated success.
- Customer booking history is now retrieved through the customer-history endpoint with a full 10-digit mobile number, rather than attempting a management endpoint or reading a potentially stale browser-wide booking cache. The API rejects partial phone searches, and Profile uses this same customer-scoped history for totals and receipts.
- Dining cards no longer label menu prices as hourly rates.
- Invalid facility-detail URLs now use the intended 404 experience instead of silently displaying Box Cricket. Customer-facing facility-rate language no longer exposes implementation terminology.
- Booking-status updates now accept only the four supported lifecycle states and return an error when the booking does not exist. SQLite now supports an explicit `SQLITE_DB_PATH`, allowing isolated integration QA without touching project data.
- Facilities now obtain sports rates from the same live pricing store as Booking and Pricing. The previously displayed sports/café hours were aligned with the then-current seed schedule only; they are not a business rule. Operating windows and slot durations must become administrator-configurable per facility, with the current values retained only as defaults until that migration is complete.
- The backend now backfills all five standard pricing tiers, including Cricket Practice Nets, and safely adds missing SQLite `details_json` metadata without overwriting saved administrator pricing. Token deposits now remain correct per facility across the API, customer UI, and admin UI.
- Management mutations no longer silently update browser state when server calls fail. Booking status, delete, pricing, and timings operations report server errors instead. A protected booking-delete endpoint was added with not-found handling.
- Public reservation creation can no longer forge `Paid` or `Confirmed` state from browser-provided fields. Public UPI submissions require a reference and are stored as `Payment Review` / `Pending verification`; authenticated staff retain the counter walk-in flow, and Razorpay verification remains the only path that marks a payment paid and a booking confirmed.
- Booking dates now reject impossible calendar dates, and only the advertised 6–9 PM slots are labelled `fast-filling`; overnight availability remains bookable without misleading scarcity messaging.
- Customer payment language now accurately distinguishes manual UPI staff review from gateway-verified confirmation. Contact phone, WhatsApp, and email details are actionable mobile links, and customer-facing inquiry/roadmap labels no longer expose internal implementation or describe live flows as upcoming.
- The UPI confirmation pass now carries the same `Payment Review` state returned by the server and uses a review indicator rather than a success checkmark, avoiding a contradictory visual confirmation.

## Current QA findings to finish

- Module 9 foundation and event API routes are committed. Disposable HTTP QA confirmed anonymous management creation returns `401` and the unseeded public list returns an intentional empty array. An authenticated disposable create request returned `500` despite the equivalent SQL succeeding directly against the same SQLite file. No diagnostic code, QA data, credentials, or temporary process artifacts are committed. Next: reproduce with request-level error capture, fix the route-layer cause, then complete CRUD/public allowlisting QA before Admin UI work.

1. Modules 6–7 public route QA is complete for locally available desktop/browser and source-level responsive checks. Perform the still-deferred true 390px physical/device-emulator pass when a supported device viewport is available; do not treat this as already executed.
2. Check every submit flow for loading, success, error, cancellation, and duplicate-submission states.
3. Verify authenticated admin workflows with configured non-production credentials and a disposable database if available.
4. Run Capacitor sync/build after Node.js is upgraded to 22 or later. Current environment is Node 20.20.2; Capacitor 8 refuses `npx cap copy android`.

## Environment notes

- Conservative repository cleanup audit completed: no source, migration, native, fallback, or public asset file was safe to remove. Tracked archive PDFs/logs remain required by archive routes and retained state; tracked SQLite database/WAL/SHM were not deleted because sidecars can contain uncheckpointed data. `.gitignore` now prevents future SQLite sidecars and generated archive delivery artifacts from being newly added without a deliberate retention migration.
- The configured cloud PostgreSQL endpoint is unreachable from this environment.
- `server/data/turf_and_taste.db-wal` and `.db-shm` were modified only by local SQLite QA startup; do not include them in a product commit.
- Use `.env.example` as the configuration template. Never commit a real `.env`.
- A local isolated SQLite server was used for this QA pass with ephemeral environment values. API checks passed: invalid partial booking-history lookup returns `400`; full mobile lookup returns `200` with only the matching history; invalid inquiry payload returns `400`.
- `npm run build`, server syntax checks, and `git diff --check` passed after the latest edits. The local browser audit verified Home’s seven-item inventory, live-price discovery cards, booking-step validation, facility detail pricing, My Bookings empty/history affordance, Profile, Contact, Inquiry, About, and authenticated admin schedule/archive UI.
- Authenticated API QA was completed against `/tmp/turf-taste-qa-5001.db`: admin login/verification and protected bookings, inquiries, and archives routes returned `200`; anonymous booking-list access returned `401`; invalid/nonexistent booking status updates returned `400`/`404` respectively.
- The `ECONNREFUSED 127.0.0.1:5000` issue was confirmed as an environment process issue, not a proxy/configuration defect: Vite correctly proxies `/api` to port 5000, and health, pricing, and slot requests return `200` once `npm run backend` is running.
- Disposable admin UI QA against `/tmp/turf-taste-ui-qa.db` verified login, all five pricing tiers and deposits, the confirmation/save workflow, booking display, overlap rejection (`409`), customer history, status update, protected delete (`200`), and missing-delete handling (`404`). A second isolated local backend verified invalid-date rejection, staff walk-in creation, public-payment-state coercion, and prime-slot status behavior; all created QA bookings were deleted afterward.

## Next safe actions

1. Begin Module 8 — Food Court & Parlour with additive food-stall/menu persistence and protected management APIs; retain existing static food presentation as the public fallback until content parity is verified.
2. Complete the deferred true 390px physical/device-emulator public responsive visual check when the tooling is available. Source-level responsive breakpoints and desktop interactive route checks are complete, but this physical viewport has not run.
3. Complete final regression in a deployment-like backend environment: configured payment gateway callback, browser back navigation, logout, and actual email/PDF archive dispatch. These require real gateway/email/storage configuration and must not be simulated.
4. Run Capacitor sync once Node is upgraded to 22+.
