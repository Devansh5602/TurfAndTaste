// Informational CMS foundation; intentionally has no production seed data.
export const id = '013_events_foundation';
export async function up({ isPostgres, exec }) {
  await exec(isPostgres ? `CREATE TABLE IF NOT EXISTS events (id VARCHAR(100) PRIMARY KEY, slug VARCHAR(120) UNIQUE NOT NULL, title VARCHAR(255) NOT NULL, short_description TEXT, description TEXT, starts_at TIMESTAMP NOT NULL, ends_at TIMESTAMP, facility_id VARCHAR(100), location_text VARCHAR(255), cover_image_url TEXT, status VARCHAR(20) NOT NULL DEFAULT 'draft', is_featured BOOLEAN NOT NULL DEFAULT FALSE, display_order INTEGER NOT NULL DEFAULT 0, created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, CHECK (ends_at IS NULL OR ends_at >= starts_at)); CREATE INDEX IF NOT EXISTS idx_events_public ON events(status, starts_at, display_order);` : `CREATE TABLE IF NOT EXISTS events (id TEXT PRIMARY KEY, slug TEXT UNIQUE NOT NULL, title TEXT NOT NULL, short_description TEXT, description TEXT, starts_at DATETIME NOT NULL, ends_at DATETIME, facility_id TEXT, location_text TEXT, cover_image_url TEXT, status TEXT NOT NULL DEFAULT 'draft', is_featured INTEGER NOT NULL DEFAULT 0, display_order INTEGER NOT NULL DEFAULT 0, created_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, updated_at DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP, CHECK (ends_at IS NULL OR ends_at >= starts_at)); CREATE INDEX IF NOT EXISTS idx_events_public ON events(status, starts_at, display_order);`);
}
